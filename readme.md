## 使いかた

1. chromedriverのインストール
```
$ brew update
$ brew install chromedriver
```
2. pipenv(jsでいうところのnpm)のインストール
```
brew install pipenv
```
3. パッケージのインストール
```
pipenv install
```
4. 実行
```
pipenv run start
```

